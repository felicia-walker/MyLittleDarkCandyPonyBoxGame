'use strict';

angular.module('ponyApp').controller('TownController', ['$scope', 'TimeService', function($scope, TimeService) {
  var self = this;
  
  self.start = function() {
    //TimeService.unpause();
    //TimeService.start();
  }
  
}]);
